<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorVibrantGreen</item>
        <item name="colorPrimaryDark">@color/colorTealBlue</item>
        <item name="colorAccent">@color/colorStoneGrey</item>
        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <item name="preferenceTheme">@style/PreferenceFixTheme.Light.NoActionBar</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background</item>
    </style>

    <style name="CustomActionBar" parent="@android:style/Widget.ActionBar">
        <item name="android:background">@color/colorVibrantGreen</item>
    </style>

</resources>
